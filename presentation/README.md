# Presentation

The deck presented on 17 September 2026. Ten slides, 16:9.

| File | Use |
|---|---|
| `slides.pdf` | The deck, the file the challenge template asks for. Ten pages, 960 x 540 points |
| `slides.md` | The slide-by-slide outline in plain text |

## Slides

1. Title
2. The problem: the valve wears out quietly, and one schedule watches everyone
3. The question: nobody predicts, they detect
4. Study design: structural deterioration, with death competing
5. What we built, data: real records in, synthetic cohorts out
6. What we built, models and technology: one inference, three probabilities
7. The comparison that decides it: beating valve age proves nothing, beat the rule
8. Results: gains where information exists, losses reported
9. What we built, the application: note in, decision support out, running on Azure
10. What comes next: an order we will not skip

## Sources

The PowerPoint original and the editable HTML source are kept outside version control, as are the two rendered
variants that preceded this one. Ask the team for them. The typefaces are Newsreader and Libre
Franklin, embedded in the source so the deck renders identically on any machine with no font
installation and no network.

## The logo

Vector assets are in `logo/`, with the specification in `logo/README.txt`. The mark is drawn inline
as SVG in the deck source, so it needs no external file.
